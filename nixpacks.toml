[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm --prefix site ci"]

[phases.build]
cmds = ["npm --prefix site run build"]

[start]
cmd = "HOST=0.0.0.0 npm --prefix site run start"
