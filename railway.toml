[build]
builder = "NIXPACKS"
buildCommand = "npm --prefix site ci && npm --prefix site run build"

[deploy]
startCommand = "HOST=0.0.0.0 npm --prefix site run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
